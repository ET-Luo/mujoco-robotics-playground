# Utility scripts

Status: documentation-only placeholder; no implementation yet.

Goal: Validate the active environment with check_env.sh.

TODO: Run bash scripts/check_env.sh from the repository root after conda activate mujoco. The script reports platform, Python, and MuJoCo information and exits nonzero on failure; it never activates conda.
